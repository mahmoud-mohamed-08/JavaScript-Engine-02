export class Circle {
    constructor(pos, r) {
        this.position = pos;
        this.radius = r;
    }

}

export class Rectangle {
    constructor(pos, width, height) {
        this.position = pos;
    }
}